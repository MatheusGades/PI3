// Função que será executada quando o botão "Comprar" for clicado
document.getElementById("comprar").addEventListener("click", function() {
    window.location.href = "TelaFinalizacao.html"; // Redireciona para a página "TelaFinalizacao.html"
});

  // Função que será executada quando o botão "Sair" for clicado
  document.getElementById("sair").addEventListener("click", function() {
    window.close(); 
 });
